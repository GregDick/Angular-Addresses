angular
  //without second argument, module just looks up an existing module as opposed to creating a new one
  .module('angularAddresses')

  .constant('API_URL', 'https://angleadresses.firebaseio.com');
